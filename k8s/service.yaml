apiVersion: v1
kind: Service
metadata:
  name: pms-service
  namespace: default
  labels:
    app: helpdesk-app
spec:
  type: NodePort
  selector:
    app: helpdesk-app
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8081
      nodePort: 30081